- hosts: localhost
  become: true
  vars:
    - version: 1.22.3
  pre_tasks:
    - name: Update cache
      apt:
        update_cache: true
      tags:
      - gotools
      - massdns
      - zsh
  tasks:
    #- include: ./core.yml
    - include_tasks: ./go-setup.yml
    - include_tasks: ./gotools.yml
    - include_tasks: ./tools.yml
 
